@model IEnumerable<SoccerStats.Models.T_Club>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nomClub)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.T_Championnat.libelleChamp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nomClub)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.T_Championnat.libelleChamp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idClub }) |
            @Html.ActionLink("Details", "Details", new { id=item.idClub }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idClub })
        </td>
    </tr>
}

</table>
