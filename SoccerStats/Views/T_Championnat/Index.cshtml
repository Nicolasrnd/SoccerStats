@model IEnumerable<SoccerStats.Models.T_Championnat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Championnat
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.libelleChamp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idChampionnat }) |
            @Html.ActionLink("Details", "Details", new { id=item.idChampionnat }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idChampionnat })
        </td>
    </tr>
}

</table>
